# homebrew-raymon

Homebrew tap for raymon.

## Install

```bash
brew install bnomei/raymon/raymon
```

## Maintain

Bump `version` and update all `sha256` values in `Formula/raymon.rb` to match the
GitHub Release tarballs from `bnomei/raymon`.

```bash
scripts/update-sha256.sh
```
